{
  "name": "uart-verilog-cocotb",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y iverilog gtkwave git-lfs && python3 -m venv --clear .venv && .venv/bin/pip install -r requirements.txt",

  "remoteEnv": {
    "VIRTUAL_ENV": "${containerWorkspaceFolder}/.venv",
    "PATH": "${containerWorkspaceFolder}/.venv/bin:${containerEnv:PATH}"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "mshr-h.veriloghdl",
        "ms-python.python",
        "lramseyer.vaporview"
      ]
    }
  }
}